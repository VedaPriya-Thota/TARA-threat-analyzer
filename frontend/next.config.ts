// ─────────────────────────────────────────────────────────────────────────────
// next.config.ts — Next.js framework configuration
//
// This file is the central configuration for the Next.js build and dev server.
// Currently no custom options are set — Next.js defaults are used.
//
// Common options you might add here in the future:
//   - reactStrictMode: true          — enables React strict mode warnings
//   - output: "standalone"           — produces a self-contained build for Docker
//   - images.remotePatterns          — whitelist external image domains
//   - env                            — expose build-time env variables to the client
// ─────────────────────────────────────────────────────────────────────────────

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Add custom Next.js config options here if needed */
};

export default nextConfig;
